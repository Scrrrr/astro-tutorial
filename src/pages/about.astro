---
import Layout from '../layouts/MinimamLayout.astro';
import Navigation from '../components/Navigation.astro';
import '../styles/global.css';
const string = "私";

const identity = {
  firstName: "サラ",
  country: "カナダ",
  occupation: "技術ライター",
  hobbies: ["写真","バードウォッチング","野球"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "ドキュメントの執筆"];
const skillColor = "blue";
const fontWeight = "lighter";
const textCase = "full-width";

const happy = true;
const finished = false;
const goal = 3;

---
<Navigation />
<Layout title="About Me.">
<body>
	<main>
		<h1>ようこそ {string}の世界へ！</h1>

    <p>以下は私に関するいくつかの事実です。</p>
    <ul>
      <li>私の名前は{identity.firstName}です。</li>
      <li>{identity.country}に住んでおり、{identity.occupation}として働いています。</li>
      {identity.hobbies.length >= 2 &&
        <li>私の2つの趣味は{identity.hobbies[0]}と{identity.hobbies[1]}です。</li>
      }
    </ul>
    <p>私のスキルは以下の通りです。</p>
    <ul>
      {skills.map((skill) => <li>{skill}</li>)}
    </ul>

    {happy && <p>幸せ～</p>}
    {finished && <p>終わりました！</p>}
    {goal == 3 ? <p>目標は{goal}日です！</p> : <p>目標は{goal}日じゃないです！</p>}

    <p>私のスキルは以下の通りです。</p>
    <ul>
      {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>

	</main>
</body>
<style define:vars={{skillColor,fontWeight,textCase}}>
  h1 {
    color: purple;
    font-size: 4rem;
  }

  .skill {
      color: var(--skillColor);
      font-weight: var(--fontWeight);
      text-transform: var(--textCase)
  }
</style>
</Layout>